# Assets File Map

## `css/`

- `app.css`: stylesheet entry point.
- `partials/`: imported CSS slices grouped by feature area.

## `js/app/`

- Split classic-script runtime for map setup, build flow, seeker flow, hider flow, and shell wiring.
- `07-bootstrap.js` is the last script loaded by `index.html`.

See:

- `css/FILEMAP.md`
- `js/app/FILEMAP.md`
